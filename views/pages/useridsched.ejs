<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@200;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <!--Header-->
    <%- include('../partials/header')%>
        <!--Main-->
        <main>
            <h1><strong>User Information</strong></h1>
            <% for (let i=0; i < usersid.length; i++){%>
            <p>First Name: <%= usersid[i].firstname %></p>
            <p>Last Name: <%= usersid[i].lastname %></p>
            <p>Email Address: <%= usersid[i].email %></p>
            <%}%>


            <table class="data">
                <tr>
                    <th>User&nbsp;ID</th>
                    <th>Day</th>
                    <th>Start&nbsp;At</th>
                    <th>End&nbsp;At</th>
                </tr>

                <% for (let i=0; i < newSchedId.length; i++){%>
                    <tr>
                        <td>
                            <%= newSchedId[i].user_id %>
                        </td>
                        <td>
                            <%= newSchedId[i].day %>
                        </td>
                        <td>
                            <%= newSchedId[i].start_at %>
                        </td>
                        <td>
                            <%= newSchedId[i].end_at %>
                        </td>
                    </tr>
                    <%}%>


            </table>
        </main>
     
</body>

</html>